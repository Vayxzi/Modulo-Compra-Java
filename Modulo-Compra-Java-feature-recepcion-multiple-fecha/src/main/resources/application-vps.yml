# src/main/resources/application-vps.yml

spring:
  datasource:
    # Apunta a la base de datos que creamos en el VPS
    url: jdbc:mysql://localhost:3306/compras_vps?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:
